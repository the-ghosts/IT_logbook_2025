# Python Syntax Summary

- Python uses indentation to define code blocks (e.g., in functions, if, loops).
- No need for semicolons at the end of lines.
- Variables are declared by assignment (e.g., name = "Michael").
- Print output using print() function.
- Comments start with #

Example:
def greet(name):
    print("Hello", name)

greet("Michael")
